apiVersion: v1
kind: Service
metadata:
  name: {{ include "goodbye-chart.fullname" . }}
  labels:
    {{- include "goodbye-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goodbye-chart.selectorLabels" . | nindent 4 }}
